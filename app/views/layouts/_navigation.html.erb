    <!-- Navigation Bar -->          
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="<%=root_url(:locale =>I18n.locale.to_s)%>">LudoWeb</a>
          <div class="nav-collapse">
            <ul class="nav">
              <li <% if @home -%>class="active"<% end -%>><a href="<%=root_url()%>"><%= t('home') %></a></li>
            </ul>
            <% if user_signed_in? -%>
	            <ul class="nav pull-right">
					<li class="dropdown">
    					<div role="button" class="btn dropdown-toggle btn-info" data-toggle="dropdown"><i class="icon-user icon-white"></i> <% if current_user.firstname != nil -%><%= current_user.firstname.capitalize %><% else -%><%= current_user.email %><% end -%>&nbsp;&nbsp;
    						<span class="caret"></span>
    					</div>
    					<ul role="menu" class="dropdown-menu">
    					    <li><%= link_to('<i class="icon-edit"></i> Edit account'.html_safe, edit_user_registration_path) %></li>
  		                    <li class="divider"></li>
    						<li><%= link_to(destroy_user_session_path,:method => :delete) do %><i class="icon-eject"></i> <%= t('logout') %><% end %></li>
    					</ul>
					</li>
				</ul>
				<p class="navbar-text pull-right">
					<%= t('connectedas') %>&nbsp;
	            </p>
	        <% else %>
	           <ul class="nav pull-right">
	               <li <% if controller_name == "sessions" -%>class="active"<% end -%>><a href="<%= new_session_path(resource_name) %>"><%= t('login') %></a></li>
	           </ul>
            <% end -%>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>